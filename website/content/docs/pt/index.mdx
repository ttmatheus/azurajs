---
title: Bem-vindo ao AzuraJS 
description: Um framework web moderno, rápido e TypeScript-first para Node.js e Bun
icon: Zap
---

# Bem-vindo ao AzuraJS ⚡

**AzuraJS** é um framework web moderno e de alta performance para Node.js e Bun que traz o poder dos decorators do TypeScript para o desenvolvimento backend. Construa APIs elegantes e type-safe com uma sintaxe limpa e intuitiva.

<Callout type="info" title="🎯 Para Quem é o AzuraJS?">
Ideal para desenvolvedores que querem criar APIs REST rápidas e robustas com TypeScript, sem a complexidade de frameworks maiores.
</Callout>

## ✨ Por que Escolher AzuraJS?

<Cards>
  <Card title="🎯 Decorators TypeScript" description="Organize seu código com decorators modernos - uma sintaxe elegante que torna suas APIs mais legíveis e fáceis de manter" />
  <Card title="🚀 Performance Excepcional" description="Projetado do zero para máxima velocidade, com overhead mínimo e zero dependências externas desnecessárias" />
  <Card title="📦 Zero Dependências" description="Leve e eficiente - apenas 1 dependência core. Sem o peso de milhares de pacotes no node_modules" />
  <Card title="🔧 TypeScript First" description="Desenvolvido pensando em TypeScript desde o início, com tipos completos, IntelliSense perfeito e segurança em tempo de desenvolvimento" />
  <Card title="🔌 Compatível com Express" description="Use middlewares e plugins do ecossistema Express - aproveite milhares de soluções prontas" />
  <Card title="⚙️ Configuração Flexível" description="Configure via arquivo TypeScript, JSON ou YAML - escolha o formato que preferir" />
</Cards>

## 🎨 Recursos Principais

AzuraJS vem com tudo que você precisa para construir APIs profissionais:

### 💉 Injeção de Parâmetros Inteligente
Decorators poderosos como `@Body`, `@Query`, `@Param`, `@Req`, `@Res`, `@Headers`, `@Cookies` e mais - acesse dados de requisição de forma elegante e type-safe.

### ✅ Validação Built-in
Sistema completo de validação de DTOs e schemas com segurança de tipos. Valide dados automaticamente antes de processar requisições.

### 🍪 Gerenciamento de Cookies
Parser e serializador de cookies nativos - trabalhe com cookies de forma simples e segura, sem pacotes extras.

### 🌐 CORS Configurável
Plugin CORS pronto para produção com configuração flexível - proteja sua API e controle acessos facilmente.

### 🛡️ Rate Limiting Integrado
Proteja sua aplicação contra abuso e DDoS com sistema de rate limiting configurável por rota.

### ⚡ Modo Cluster
Aproveite todos os núcleos do processador com suporte cluster nativo - maximize performance em produção.

### 📊 Logging Contextual
Sistema de logging inteligente que se adapta ao ambiente - verbose em desenvolvimento, conciso em produção.

### 🚨 Tratamento de Erros Robusto
Classe `HttpError` e middleware de erro para tratamento estruturado e consistente de exceções.

## 🚀 Veja Como é Simples

Aqui está um exemplo completo de uma API CRUD em menos de 30 linhas:

```typescript
import { AzuraClient } from "azurajs";
import { applyDecorators } from "azurajs/decorators";
import { Controller, Get, Post, Body, Param } from "azurajs/decorators";

@Controller("/api/users")
class UserController {
  // GET /api/users - Lista todos os usuários
  @Get()
  getAllUsers() {
    return { users: ["Alice", "Bob", "Charlie"] };
  }

  // GET /api/users/:id - Busca usuário por ID
  @Get("/:id")
  getUser(@Param("id") id: string) {
    return { id, name: `Usuário ${id}` };
  }

  // POST /api/users - Cria novo usuário
  @Post()
  createUser(@Body() data: any) {
    return { 
      id: Date.now(), 
      ...data,
      createdAt: new Date().toISOString()
    };
  }
}

// Inicializa e registra os controllers
const app = new AzuraClient();
applyDecorators(app, [UserController]);

// Inicia o servidor
await app.listen(3000);
console.log("🚀 API rodando em http://localhost:3000");
```

<Callout type="success">
**Pronto!** Sua API já está funcionando com rotas REST completas, validação de tipos e estrutura organizada.
</Callout>

## 📚 Próximos Passos

Comece sua jornada com AzuraJS seguindo estes guias:

<Cards>
  <Card 
    title="📦 Instalação" 
    href="pt/installation" 
    description="Instale o AzuraJS e configure seu ambiente de desenvolvimento em minutos" 
  />
  <Card 
    title="🚀 Início Rápido" 
    href="pt/quick-start" 
    description="Crie sua primeira API REST completa seguindo nosso tutorial passo a passo" 
  />
  <Card 
    title="⚙️ Configuração" 
    href="pt/configuration" 
    description="Explore todas as opções de configuração e personalize seu servidor" 
  />
  <Card 
    title="🎯 Controllers" 
    href="pt/controllers" 
    description="Domine o padrão de controllers e organize seu código de forma profissional" 
  />
</Cards>

---

## 💡 Principais Diferenças

### AzuraJS vs Express

<Callout type="tip">
**Express** é ótimo, mas AzuraJS adiciona:
- 🎯 Decorators TypeScript para código mais limpo
- 📦 Zero dependências desnecessárias
- ✅ Validação nativa e type-safe
- 🚀 Melhor performance out-of-the-box
- 🔧 Configuração centralizada e estruturada
</Callout>

### AzuraJS vs NestJS

<Callout type="tip">
**NestJS** é poderoso, mas AzuraJS oferece:
- 🪶 Muito mais leve (sem overhead de Angular/RxJS)
- 🎨 Sintaxe mais simples e direta
- ⚡ Startup mais rápido
- 📝 Curva de aprendizado menor
- 🎯 Foco em APIs, não em frameworks complexos
</Callout>

## 🌟 Comunidade

AzuraJS é código aberto e aceita contribuições! Junte-se à nossa comunidade:

- ⭐ [Repositório GitHub](https://github.com/0xviny/azurajs)
- 📦 [Pacote NPM](https://www.npmjs.com/package/azurajs)
- 🐛 [Reportar Issues](https://github.com/0xviny/azurajs/issues)
- 💬 [Discussões](https://github.com/0xviny/azurajs/discussions)

---

Pronto para construir algo incrível? Vamos começar! 🎉
