---
title: Instalação
description: Instale AzuraJS no seu projeto em minutos
icon: Package
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Instalação 📦

Comece com AzuraJS instalando-o no seu projeto Node.js ou Bun. Este guia vai te levar do zero até uma aplicação funcionando em poucos minutos.

## Pré-requisitos ✅

Antes de começar, você vai precisar de:

<Callout type="warn" title="Requisitos Mínimos">
- **Node.js** 18+ ou **Bun** 1.0+
- **TypeScript** 5.0+ (recomendado para usar decorators)
- Um gerenciador de pacotes: npm, yarn, pnpm ou bun
</Callout>

### Verificando sua versão

```bash
# Verificar Node.js
node --version  # deve ser v18.0.0 ou superior

# Verificar TypeScript (se instalado globalmente)
tsc --version   # deve ser 5.0.0 ou superior

# Verificar Bun (se estiver usando)
bun --version   # deve ser 1.0.0 ou superior
```

## Instalação do Pacote 📥

Escolha seu gerenciador de pacotes favorito e instale o AzuraJS:

<Tabs items={['npm', 'yarn', 'pnpm', 'bun']}>
  <Tab value="npm">
    ```bash
    npm install azurajs
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add azurajs
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add azurajs
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add azurajs
    ```
  </Tab>
</Tabs>

<Callout type="success">
**Pronto!** O AzuraJS tem apenas 1 dependência core, então a instalação é super rápida. 🚀
</Callout>

## Configuração TypeScript 🔧

AzuraJS usa decorators do TypeScript, então precisamos configurar o compilador corretamente.

### Configuração Recomendada

Crie ou atualize seu `tsconfig.json` com estas configurações:

```json title="tsconfig.json"
{
  "compilerOptions": {
    // Configurações necessárias para decorators
    "experimentalDecorators": true,      // ⚠️ OBRIGATÓRIO
    "emitDecoratorMetadata": true,       // ⚠️ OBRIGATÓRIO
    
    // Configurações recomendadas
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",       // ou "node16"
    
    // Qualidade de código
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    
    // Output
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

### Entendendo as Configurações

<Callout type="info" title="experimentalDecorators">
**O que faz:** Habilita o suporte a decorators do TypeScript.

**Por que é necessário:** Sem isso, `@Controller`, `@Get`, `@Post` e outros decorators não funcionarão.
</Callout>

<Callout type="info" title="emitDecoratorMetadata">
**O que faz:** Permite que o TypeScript emita metadados sobre os decorators.

**Por que é recomendado:** Necessário para recursos avançados como injeção de dependências e validação automática de tipos.
</Callout>

<Callout type="tip">
**Dica:** Se você estiver usando Node.js, pode usar `"moduleResolution": "node16"` ao invés de `"bundler"`.
</Callout>

## Configuração do Projeto 🏗️

Vamos criar uma estrutura de projeto organizada do zero:

### Passo 1: Criar o Diretório

```bash
mkdir meu-app-azura
cd meu-app-azura
```

### Passo 2: Inicializar o Projeto

```bash
npm init -y
```

### Passo 3: Instalar AzuraJS

```bash
npm install azurajs
```

### Passo 4: Instalar Dependências de Desenvolvimento

```bash
npm install -D typescript @types/node tsx
```

### Passo 5: Criar Estrutura de Pastas

```bash
mkdir -p src/controllers
```

### Estrutura Final

Seu projeto deve ter esta estrutura:

```
meu-app-azura/
├── src/
│   ├── index.ts              # Arquivo principal do servidor
│   └── controllers/
│       └── AppController.ts  # Seu primeiro controller
├── azura.config.ts           # Configuração do AzuraJS
├── tsconfig.json             # Configuração do TypeScript
└── package.json              # Dependências do projeto
```

<Callout type="tip" title="🎯 Dica de Organização">
Esta estrutura é recomendada, mas você pode organizá-la como preferir. O importante é manter os controllers separados e o código bem organizado.
</Callout>

## Verificar Instalação ✔️

Vamos criar um teste rápido para garantir que tudo está funcionando:

### Criar Arquivo de Teste

Crie o arquivo `src/index.ts`:

```typescript title="src/index.ts"
import { AzuraClient } from "azurajs";

const app = new AzuraClient();

console.log("✅ AzuraJS instalado com sucesso!");
console.log("📦 Versão:", require("azurajs/package.json").version);
console.log("🚀 Pronto para começar!");
```

### Executar o Teste

<Tabs items={['Node.js', 'Bun']}>
  <Tab value="Node.js">
    ```bash
    npx tsx src/index.ts
    ```
  </Tab>
  <Tab value="Bun">
    ```bash
    bun run src/index.ts
    ```
  </Tab>
</Tabs>

### Resultado Esperado

Se tudo estiver correto, você verá:

```
✅ AzuraJS instalado com sucesso!
📦 Versão: 2.x.x
🚀 Pronto para começar!
```

<Callout type="success">
**Parabéns!** 🎉 Você instalou o AzuraJS com sucesso. Agora está pronto para construir sua primeira API!
</Callout>

## Ferramentas de Desenvolvimento (Opcional) 🛠️

Para uma experiência de desenvolvimento melhor, instale estas ferramentas:

### Essenciais

```bash
# TypeScript e definições de tipos
npm install -D typescript @types/node

# Executor TypeScript (recomendado)
npm install -D tsx
```

### Hot Reload / Watch Mode

<Tabs items={['tsx + nodemon', 'Bun Watch', 'tsx watch']}>
  <Tab value="tsx + nodemon">
    ```bash
    npm install -D tsx nodemon
    ```
    
    Crie `nodemon.json`:
    ```json
    {
      "watch": ["src"],
      "ext": "ts",
      "exec": "tsx src/index.ts"
    }
    ```
    
    Adicione ao `package.json`:
    ```json
    "scripts": {
      "dev": "nodemon"
    }
    ```
  </Tab>
  
  <Tab value="Bun Watch">
    ```bash
    # Nenhuma instalação necessária!
    bun --watch src/index.ts
    ```
    
    Adicione ao `package.json`:
    ```json
    "scripts": {
      "dev": "bun --watch src/index.ts"
    }
    ```
  </Tab>
  
  <Tab value="tsx watch">
    ```bash
    npm install -D tsx
    ```
    
    Adicione ao `package.json`:
    ```json
    "scripts": {
      "dev": "tsx watch src/index.ts"
    }
    ```
  </Tab>
</Tabs>

### Scripts Úteis

Adicione estes scripts ao seu `package.json`:

```json title="package.json"
{
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit"
  }
}
```

<Callout type="info">
**Dica:** Use `npm run dev` para desenvolvimento com reload automático e `npm start` para produção.
</Callout>

## Próximos Passos 🚀

Agora que AzuraJS está instalado, você está pronto para:

<Cards>
  <Card title="Início Rápido" href="quick-start" description="Construa sua primeira API" />
  <Card title="Configuração" href="configuration" description="Configure sua aplicação" />
  <Card title="Controllers" href="controllers" description="Aprenda sobre controllers" />
</Cards>

## Solução de Problemas 🔍

Encontrou algum problema? Aqui estão as soluções para os problemas mais comuns:

### ❌ Erro: "Decorator não é reconhecido"

**Problema:** Decorators como `@Controller` causam erro de sintaxe.

**Solução:**

1. Verifique o `tsconfig.json`:
   ```json
   {
     "compilerOptions": {
       "experimentalDecorators": true,
       "emitDecoratorMetadata": true
     }
   }
   ```

2. Confirme a versão do TypeScript:
   ```bash
   npx tsc --version  # deve ser 5.0.0+
   ```

3. Reinicie seu editor (VS Code, etc.)

### ❌ Erro: "Cannot find module 'azurajs'"

**Problema:** TypeScript não encontra o módulo AzuraJS.

**Solução:**

1. Verifique se está instalado:
   ```bash
   npm list azurajs
   ```

2. Reinstale se necessário:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. Verifique o `moduleResolution` no tsconfig:
   ```json
   {
     "compilerOptions": {
       "moduleResolution": "bundler"  // ou "node16"
     }
   }
   ```

### ❌ Erros de Tipo

**Problema:** TypeScript reclama de tipos incompatíveis.

**Solução:**

1. Instale definições de tipos:
   ```bash
   npm install -D @types/node
   ```

2. Configure o tsconfig corretamente:
   ```json
   {
     "compilerOptions": {
       "esModuleInterop": true,
       "skipLibCheck": true
     }
   }
   ```

3. Limpe o cache do TypeScript:
   ```bash
   rm -rf .tsbuildinfo dist
   npx tsc --build --clean
   ```

### ❌ Porta já em uso

**Problema:** Erro `EADDRINUSE: address already in use`.

**Solução:**

<Tabs items={['Windows', 'Linux/Mac']}>
  <Tab value="Windows">
    ```bash
    # Encontrar o processo usando a porta
    netstat -ano | findstr :3000
    
    # Matar o processo (substitua PID)
    taskkill /PID <PID> /F
    ```
  </Tab>
  
  <Tab value="Linux/Mac">
    ```bash
    # Encontrar e matar o processo
    lsof -ti:3000 | xargs kill -9
    ```
  </Tab>
</Tabs>

### ❌ Import path não funciona

**Problema:** `import { X } from "azurajs/decorators"` não funciona.

**Solução:**

Certifique-se de usar o caminho correto:

```typescript
// ✅ Correto
import { Controller, Get } from "azurajs/decorators";
import { AzuraClient } from "azurajs";

// ❌ Incorreto  
import { Controller } from "azurajs";  // não funciona
```

<Callout type="warn" title="Ainda com problemas?">
Se nenhuma dessas soluções funcionou:

1. 🐛 [Reporte um bug no GitHub](https://github.com/0xviny/azurajs/issues)
2. 💬 [Pergunte nas Discussões](https://github.com/0xviny/azurajs/discussions)
3. 📚 [Confira os exemplos](https://github.com/0xviny/azurajs/tree/main/examples)
</Callout>
